import { PlaceHolderImages as appImages, type ImagePlaceholder as appType } from '@/app/lib/placeholder-images';

export type ImagePlaceholder = appType;

export const PlaceHolderImages: ImagePlaceholder[] = appImages;
